% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ltrc_tests.R
\name{ltrc.test}
\alias{ltrc.test}
\title{Tests for the \eqn{k}-sample problem under left truncation
and right censoring}
\usage{
ltrc.test(
  p.sample,
  weights = NULL,
  tests = c("ks", "cvm", "logrank"),
  B = 500,
  plot.curves = FALSE,
  holes = c("holes", "add", "conditional"),
  p = 0,
  q = 0,
  seed = NULL,
  keep.boot = FALSE
)
}
\arguments{
\item{p.sample}{Matrix or data frame with columns (U, Y, \eqn{\Delta}, group).}

\item{weights}{Vector of weights of the estimator under the null hypothesis.
If no vector is provided, weights proportional to each sample size are assigned automatically
by the function. Not used when \code{tests="logrank"}.}

\item{tests}{Tests to perform. One of Kolmogorov--Smirnov (ks), Cramér--von Mises (cvm),
or weighted log-rank-type tests (logrank).}

\item{B}{Number of bootstrap replications to approximate the \eqn{p}-values of the Kolmogorov--Smirnov
and/or Cramér--von Mises tests.}

\item{plot.curves}{Logical, FALSE by default. If set to TRUE, the \eqn{k} survival functions
to be compared are depicted.}

\item{holes}{One of \code{holes} (default), \code{conditional} or \code{add}. If \code{holes = "add"} and \code{logrank}
is requested, the log-rank test is computed with \code{holes = "holes"} and a warning is issued. See
\code{\link[=ltrc.estimator]{ltrc.estimator()}} for details.}

\item{p, q}{Weights of the log-rank-type tests. The choice \eqn{p=0=q} yields the classical
log-rank test.}

\item{seed}{Seed for the bootstrap resamples.}

\item{keep.boot}{Logical. If \code{TRUE}, bootstrap replicates are included in the
returned object as \code{stat.boot}. Default is \code{FALSE}.}
}
\value{
A list with the following components:
\itemize{
\item \code{statistic}: numerical value of the Kolmogorov--Smirnov and/or Cramér--von Mises statistics.
\item \code{p.value}: approximated p-value of the tests.
\item \code{stat.boot}: bootstrap statistics for the p-value approximation.
\item \code{logrank}: a matrix with weights \code{p} and \code{q}, test statistics, and \eqn{p}-values.
\item \code{B}: number of bootstrap replications.
\item \code{tests}: tests performed, as specified in the input.
\item \code{curves}: distribution function estimators of the \eqn{k} samples to be compared.
\item \code{groups}: group indicators.
\item \code{holes}: method for handling holes.
}

The \code{summary} method returns a data frame with one row per test
and the following columns:
\describe{
\item{Test}{Name of the test. For the weighted log-rank-type tests, the weights
\code{p} and \code{q} are detailed.}
\item{Statistic}{Observed value of the test statistic.}
\item{p.value}{Corresponding \eqn{p}-value.}
}
}
\description{
Computes \eqn{k}-sample tests adapted to left truncation and right
censoring and the corresponding \eqn{p}-value.
}
\details{
This function collects the three tests implemented in this package: Kolmogorov--Smirnov,
Cramér--von Mises (Lago et al., 2026) and weighted log-rank-type tests, (Klein and Moeschberger, 2003)
adapted to left truncation and right censoring. The \eqn{p}-values of the first two are
approximated by bootstrap, and the \eqn{p}-value of the log-rank tests is computed by
the \eqn{\chi^2_{k-1}} asymptotic distribution of the test statistic, where \eqn{k}
is the number of groups.
}
\examples{
if (requireNamespace("KMsurv", quietly = TRUE)) {
data("channing", package = "KMsurv")

data.channing <- data.frame(channing$ageentry, channing$age,
channing$death, channing$gender)
aa <- ltrc.test(data.channing,weights=rep(1/2,2),
holes='conditional',tests=c('ks','cvm','logrank'), B = 200,
p=c(0,1,0.5,0),q=c(0,0,0.5,1))
plot(aa)
summary(aa)
}

}
\references{
Klein, J.P., and Moeschberger, M. L. (2003). Survival Analysis: Techniques for Censored
and Truncated Data. Springer, New York.

Lago, A., Pardo-Fernández, J.C., and de Uña-Álvarez, J. (2026). Kolmogorov--Smirnov
and Cramér--von Mises tests for the k-sample problem for left-truncated and
right-censored data. Lifetime Data Analysis: 32(2).
}
